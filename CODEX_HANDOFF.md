# Daybook 项目交接

更新：2026-09-11。本次按用户确认新增首次通知授权、可跳过提醒设置引导及多品牌后台设置说明；只做相关专项，未重跑完整 QA。新对话请以当前文件和 Git 实际状态为准。

## 1. 当前目标与状态

Daybook 是可持续维护的离线安卓个人日历，统一日程、任务和生活记录。用户以自己和朋友的实际使用需求驱动开发，同时学习 AI coding 与工程维护；不是一次性交付项目。

**v0.3.0 已发布；主分支已实现首次提醒设置改进，尚未发布新版本。** 本轮范围由用户确认，已完成实现；没有获准启动的其他 backlog。版本号仍为 0.3.0，不能把主分支新行为当成已发布 APK 的行为。

- 仓库：https://github.com/lukino0737/daybook（公开）。
- 发布：https://github.com/lukino0737/daybook/releases/tag/v0.3.0。
- 用户新增真机反馈：Redmi K70 Pro 开启自启动后，「智能限制后台运行」和「无限制」两种省电策略均能在手动划掉后台后正常提醒；关闭自启动后需回到前台才补发。未提供具体系统版本，不能推广到其他机型或称所有真机验收完成。
- 本轮未改变补发提醒机制，只新增授权与设置引导。

## 2. 已完成阶段

- v0.1：单模块安卓工程、三类记录增删改、完成/恢复、删除短时撤销、月历/DDL、本地持久化、编辑草稿、JSON 导出与替换恢复/快照、签名 APK 与持续托管。
- v0.2：独立本地提醒、通知点击打开记录、自动调度和生命周期恢复、标签、回顾与标题/正文搜索、增量迁移、固定竖屏。
- v0.3 A（3a92819）：固定底部日历/任务/回顾三入口，任务数量和有无 DDL 分区，紧凑日历与左右滑动，条件显示今天/日期差，加号按钮；日程命名、叉号清除任务日期；显式回顾筛选。
- v0.3 B（036408a）：内置 2025/2026 中国大陆官方放假调休，常用公历及农历节日；蓝点放假、红点补班、绿点个人事项，日期详情及辅助功能描述。
- v0.3 C（1382f83）：独立提醒设置页及返回键，删除重复权限文案与手动重新检查，保留自动检查；旧渠道保持原设置，新渠道默认系统声、振动、高重要性。
- v0.3 交付（827709a）：同签名升级、Release 验收、README 新截图、使用与测试文档、APK 发布；发布记录提交 bbbaa0f。

- 未发布的首次提醒设置：Android 13+ 首次运行且未授权时自动请求一次通知权限，允许/拒绝/关闭弹窗后接一次可跳过引导；Android 8–12 或已授权时直接引导。选去设置、稍后设置或返回后不重复；旧安装升级后也有一次新增引导。
- 提醒页增加自启动与后台运行、悬浮通知建议；小米/红米/POCO、华为/荣耀、OPPO/一加/realme、vivo/iQOO 有品牌说明与尝试直达入口，三星提示检查休眠应用，其他品牌使用通用说明。所有跳转都有应用详情/系统设置回退；不读取或猜测厂商权限状态。

## 3. 已批准的重要设计决策

- Kotlin / Jetpack Compose / Room / ViewModel / StateFlow；单 Android 模块，Repository 隔离数据库。Android 8.0+，单人本地离线使用，无账号、联网权限或统计服务。
- 界面简洁、少说明文字；保留纸面风格和「把日子，记在一起。」等原有文案，只改用户指定部分。手机固定竖屏。
- 类型固定为日程、任务、记录；标签是独立主题分类，可跨类型。日程仅更改显示名，内部 EVENT 标识不变。
- 默认填入选中日期；日程和记录必须有日期，仅任务可用叉号同时清除日期/时间。仅任务可以完成或逾期，只有日期的 DDL 在当天结束后逾期。
- 底部仅日历、任务、回顾。任务数量统计所有未完成任务，有 DDL 在上、无 DDL 在下；完成项仍可从日历/回顾找回。
- 回顾默认全部类型/标签/时间；自定义范围两端包含，结束默认今天，无日期任务仅在全时间段出现。点筛选才应用，全默认先确认；编辑条件不立即改变旧结果。
- 日期采用本地日历语义。提醒时间独立于发生/截止日期，修改后者不自动移动提醒。
- 节假日随 APK 内置；农历转换用系统 ICU，以中国时区解释日历日期。未覆盖年份不猜测放假/补班。
- 用户明确选择保留 `daybook.reminders` 通知渠道，已有渠道由系统/用户管理，不重建渠道绕过旧设置。新安装才应用新的默认振动/声音配置。
- Room schema 3、JSON 格式 3 沿用 v0.2；禁止破坏性迁移。备份完整校验→确认数量及替换→本地快照→事务替换，失败不损坏原数据，不合并多份备份。
- 继续使用原签名直接覆盖安装；密钥不可提交、不可输出，不让用户卸载来解决升级问题。

### 补发提醒现行规则

只处理未完成、启用提醒且尚未发送的日程/任务；通知与准时提醒权限都满足时，原定时刻已到且距离现在**不足 24 小时**才发送。满 24 小时不补发。晚于原定时间 60 秒以上标注「补发提醒」。应用启动/返回前台、数据变化、系统闹钟、开机及相关系统事件触发核对。

发出状态保存在记录中，用于避免重复；用户未阅读、划掉通知或没听到提示不会导致再次补发。这里的“已发出”表示已交给系统通知接口，不代表用户确实看到。实现见 ReminderRules 与 ReminderCoordinator。

## 4. 已废弃交互与本版不采用方案

- 旧「未安排/无截止」独立入口及页面已移除，不恢复；无日期任务合入任务页。
- 类型显示名「安排」改为「日程」；回顾不再只提供「生活记录/全部类型」，而是完整列出固定类型。
- 不再使用顶部导航按钮、大日期格里的事项标题、左右月份箭头、「记一笔」文字悬浮按钮、始终显示的今天按钮。
- 回顾不再随每次输入立即刷新，也不默认仅看生活记录。
- 不再在多个页面重复展示提醒权限已开启状态，不再提供手动「重新检查提醒」按钮；自动核对仍保留。
- 本版未采用联网获取节假日或另建通知渠道；这是当前版本的已确认选择，并非永久禁止未来重新讨论。
- 完整图文教程/回看入口仍留后续；本轮仅批准并实现一次性提醒设置引导。生日重复、照片、自定义背景、生理期专用能力、同步、AI 录入等只是候选方向，尚未批准实施方案。

## 5. 已知问题、风险与未完成验收

- 没有已确认而未修复的 v0.3 发布阻塞 bug；仍需接收实际使用反馈，不能理解为所有情况均无 bug。
- 新增引导尚未在品牌真机或 Android 8–12 实测。Redmi K70 Pro 的既有提醒与自启动反馈见第 1 节，其他品牌后台行为，以及声音、振动、悬浮及静音/勿扰仍未完成全面实测。系统强行停止不同于进程回收，强行停止后需重开 App。
- 旧安装可能保留振动关闭等设置，需要用户从独立提醒页进入系统设置调整；这是已批准的渠道保留方案。
- 官方调休表和清明名称仅覆盖 2025/2026；年度更新需维护资料，不应将节日名称当成放假状态。
- v0.3 本轮验证了后台进程退出后提醒，系统重启场景在 v0.2 验收过，本轮未重复完整重启专项。
- 大屏系统可能忽略竖屏请求；没有专门横屏布局。图文教程和两周试用效果评价未完成。
- GitHub Actions 有版本弃用提示，当前 CI 成功；升级这些依赖不属于本次已批准范围。
- 已解决的测试故障：日历标记需读取未合并语义；大字体回顾测试原来点击合并卡片中心，落在标签上，改点标题本身后通过。不是通过删用户数据规避问题。

## 6. 已验证结果与证据

本节下方为已发布 v0.3.0 的既有证据，本轮复用。未发布改动已通过 Debug/测试 APK 构建、Lint（0 错误）、3 项品牌 JVM 测试、8 项相关仪器测试、真实授权允许/拒绝/关闭三条路径和 2 项 150% 字体专项。详见 docs/TESTING.md 开头，日志 work/startup-*.log；不要重复完整 QA。

- 32 项 JVM 单测、Debug/Release 构建、Lint 通过；Lint 0 错误。
- 常规模拟器运行 22 项，其中 19 项通过、3 项条件专项跳过；不能把 `OK (22 tests)` 当成 22 项均执行通过。
- 通知权限拒绝独立通过；150% 字体及软键盘下提醒设置、编辑、回顾共 3 项通过，回顾定位修正后独立复测。
- 真实已发布 v0.2 → v0.3 同签名覆盖升级通过：记录 ID、日期时间、完成状态、标签、提醒/已发送状态、创建修改时间、EVENT 身份与原渠道设置保留。
- Release 新安装两项 UI 用例通过；系统实际渠道为重要性 4、系统通知声、振动开启。
- 确认后台进程退出、不重开 App 后，系统实际发出通知；截图和 SystemUI HeadsUp 状态确认顶部悬浮，重启 App 后核对已发送状态通过。模拟器宿主音频关闭，不能声称听感/振感实测通过。
- README 月历、回顾、提醒设置截图已更新并目视检查，只含虚构事项。
- 发布代码 GitHub CI：https://github.com/lukino0737/daybook/actions/runs/34560985624，结果通过。
- 详细验收：docs/TESTING.md；原始记录 work/v03-reminder-build.log、v03-regression-device.log、v03-denied-device.log、v03-large-font-device.log、v03-large-font-retest.log、v03-release-build.log、v03-upgrade.log、v03-release-ui.log、v03-lifecycle-*.log。

## 7. Git 与交付物

- 本轮接手时 `main` 与本地 origin/main 为 `1796e76`，工作区干净；当前 HEAD 请以 `git log -1` 为准，已发布标签保持原位。
- 最近稳定发布代码：`827709a3d132b3b255f1107c8a3700fa0b1e7c72`，标签 `v0.3.0`。不要移动已发布标签或强推。
- 本次包含未发布业务代码、对应测试与文档，完成后作为一个里程碑提交并推送 main；没有改版本号、移动标签或上传新 Release。
- APK：outputs/daybook-v0.3.0.apk（约 12 MB）。SHA-256：`1fdd6fedb3fb99ccb720d3f03eaf3fda94bad763eef8748edab0db050e875fa1`，交接时重算一致。
- 校验文件：outputs/SHA256SUMS-v0.3.0.txt。发布时远程 APK/校验文件 digest 已核对；证据 work/v03-published-release.json，交接时读取本地证据，未重新联网验收。
- 源码归档：outputs/daybook-v0.3.0-source.zip，来自发布代码，不含之后仅文档的交接补充；最新交接应读取本工作区或 main。
- 其他：outputs/使用说明-v0.3.0.md、验收报告-v0.3.0.md、v03-heads-up.png；旧版 APK 保留用于后续需要时的升级测试。
- 本轮实际启动专用模拟器时仍发现旧 v0.3.0 Release 安装，与上次“已卸载”的文字不符；已使用同签名 QA 包覆盖，保留日历数据。仅重置本轮新增引导偏好和测试通知授权；未卸载 App、未清空数据库。收尾关闭专用模拟器，保留 QA 安装供恢复。

## 8. 关键文件与环境

- AGENTS.md：工程约定；README.md：入口与下载；docs/DEVELOPMENT.md：各里程碑；docs/V0.3-PLAN.md：已批准范围；docs/BACKLOG.md：候选需求；docs/USAGE.md、BUILD.md、TESTING.md：使用/构建/验收；docs/HOLIDAY-SOURCES.md：官方日期来源。
- app/src/main/java/dev/lukino/daybook/：data/（实体、Repository、日期与筛选规则）、ui/（日历、表单、回顾、提醒页）、calendar/（假期表与农历）、reminder/（调度与接收）、backup/（格式校验/快照）。MainActivity 保留返回前台自动核对。
- app/src/test/、app/src/androidTest/：测试；app/schemas/：历史 Room schema；docs/screenshots/：公开样例截图；docs/releases/v0.3.0.md：发布说明。
- app/build.gradle.kts：versionCode 3 / versionName 0.3.0。Gradle 8.13、AGP 8.13.2、Kotlin 2.2.21、Compose BOM 2025.12.01、Room 2.8.4；具体见 BUILD.md。
- 本机可复用工具：JAVA_HOME=$PWD/work/tooling/jdk-21.0.12.1.jdk/Contents/Home；GRADLE_USER_HOME=$PWD/work/gradle-user；ANDROID_USER_HOME=$PWD/work/android-user；ANDROID_HOME=$PWD/work/android-sdk。
- 模拟器仅使用专用 Daybook_API_35，启动时 ANDROID_AVD_HOME=$PWD/work/avd；未授权不要操作用户手机或其他模拟器。
- 新增关键文件：ui/StartupReminderSetup.kt（独立 reminder_setup 偏好，不进 JSON）、reminder/BackgroundSettingsProfile.kt、ReminderSettingsNavigation.kt；StartupReminderSetupTest、ReminderSettingsNavigationTest、BackgroundSettingsProfileTest。
- 本轮验证脚本与截图：work/startup-qa/，签名 QA APK 只用于专用模拟器，不是新正式版；实际通知授权允许/拒绝/关闭弹窗的自动 UI 检查保存于 check_permission.py。
- work/github_cli.py：已有 GitHub CLI 认证包装；work/verify_upgrade_v03.py：已完成的升级驱动。不要为恢复直接重跑会清理专用模拟器安装的脚本。
- `.local/signing/daybook-release.jks`、`keystore.properties` 交接时确认存在，未读取凭据；必须保持忽略，不打印、不提交。work/、outputs/、local.properties、构建产物不入库。发布 APK 用 Release 附件。

## 9. 用户协作约定与下一阶段优先级

- 中文沟通，改动小而可解释；每个里程碑说明目的、行为、验证和学习入口，更新 DEVELOPMENT，检查暂存后提交并推送 main，核对远程。不强推，不覆盖历史。
- **宏观方案先确认**：涉及方向、范围、架构、主要功能、数据组织、依赖或交付方式，应先提供建议方案、至少一个替代方案、优缺点/风险及推荐理由；用户确认最终方案后再执行。批准范围内的微观实现和普通修复可自主完成。尤其不要擅自改入口名称或产品行为。
- 用户说“先记下”时只接收，等明确要求再整理/实施；不要边收集反馈边开发。本轮首次提醒设置方案已获明确确认并实施；不要延伸到完整教程或新功能。
- 长任务适度检查额度：>10% 正常；约 5–10% 收尾当前小阶段，不开新大阶段；<5% 或不足安全完成下一阶段时保存交接并暂停。不得未经授权使用额度重置。恢复先查状态，复用已有成果。
- P0：等用户 v0.3 真机反馈；有可复现的数据/通知送达问题时优先诊断与针对性修复，同签名补丁发布。
- P1：收集日历/筛选操作和提醒设置体验；按用户确认的小范围反馈改进，继续保持简洁。安排后续年度假期资料维护。
- P2：完整教程、生日重复、照片、背景、生理期记录、同步、AI 录入等候选项目，再讨论优先级与方案；尚未获准启动。

## 10. 新对话先做什么

1. 确认正在正确仓库，读本文件和 AGENTS.md；看 `git status --short --branch`、最近日志。区分已发布代码、后续文档提交及新出现的未提交改动。
2. 对照用户最新请求；若没有新实施任务，简短说明接手状态并等待，不自行启动 backlog。
3. 只读相关代码和对应验收日志。已通过部分不重复生成、不全面重测；状态有差异先说明，再做低成本核对。
4. 有新范围先走方案确认；需要开发才检查额度与工具权限。网络/沙盒授权不假设跨对话继承；已有认证/密钥可复用但不得泄露。
5. 保留原签名、历史 schema、旧 APK 与既有发布标签。只有实际变更或明确疑点才触发必要的针对性测试和新发布。
