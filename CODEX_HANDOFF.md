# Daybook 项目交接

更新：2026-09-15。当前 v0.5.0/code6 已完成业务实现与本地验收，业务074370d已推送，正在修复CI准备环境并交付安装包。尚未完成远程发布。不要重复构建或全部QA，先核对本文与Git/已有输出。

## 当前目标与授权

用户确认方案A（docs/V0.5-M2.md），保持0.5.0，完成后发布0.5.0安装包。后续明确要求删除空正文弹窗的「继续编辑」选项，已落实。外观里程碑329c64e直接复用；本轮新增任务/便签交互、日历默认类型和指定列表左滑删除。宏观方向仍需先说明方案与替代方向并由用户确认；批准范围内细节自主完成。

项目为离线安卓个人日历，统一日程、截止任务、生活记录，并有独立便签。仓库 https://github.com/lukino0737/daybook 。原已发布 v0.4.0 完成收尾，其标签与附件不得改动。此次0.5.0安装包发布已有授权，不应按旧交接重复索要安装包授权或回退为0.4.1。

## 已完成

- 历史：v0.1基础/备份，v0.2提醒/标签，v0.3四类使用路径相关日历优化，v0.4独立便签、背景主题、筛选重置、首次提醒引导。细节见 docs/DEVELOPMENT.md、TESTING.md、V0.4-PLAN.md。
- v0.5外观里程碑329c64e：相册权限、应用内相册分组选图、部分授权补选/拒绝回退、完整调色盘、无背景独立配色、切回背景取色、删除指定说明。41项JVM与7项外观专项及CI34822871560通过。
- 本轮：任务页在有/无截止日期下面增加默认收起的已完成分区，最近修改倒序；状态先保存，勾选/划线/颜色渐变及400毫秒过渡后归类，反选恢复。只有任务可以完成和逾期，导航数字只计未完成。
- 任务页全部任务、便签、日历当天三类事项支持左滑露出删除按钮，点击后二次确认，一次展开一条；记录类原有撤销保留。回顾和近期截止卡片不扩展手势。提供无障碍删除操作。
- 日历选过去日期新建默认记录，今天和未来仍默认日程，以点击时本地日期判断；其他入口和已有事项类型不变。
- 便签顶部完成保存退出，删除移到底部，自动保存保留；新建空便签（包括输入后清空）取消创建；已有便签清空时只提供保留上次保存内容退出或删除，删除再确认。失败保留内容。
- 正式配置0.5.0/code6，原签名；Room4/JSON4保持，无新依赖。README、USAGE、TESTING、V0.5-M2、DEVELOPMENT和发布说明已更新。

## 已验证与证据

- 43项JVM测试通过，Lint0错误；Debug/AndroidTest/Release构建通过。最后业务Release构建 work/v05/release-final-build.log，新增升级测试编译 upgrade-test-build.log。
- API35 ARM64专用模拟器：正常字号15项专项分批通过（交互6、任务列表1、便签UI1、便签存储2、备份UI2、迁移2、视觉预览1），另1项150%字号交互预览通过。用户要求的两选项弹窗、空白/保存失败/删除失败/恢复/撤销均有验证。
- 正常组历史记录 work/v05/acceptance-normal.log 为14项通过、日历1项失败；修正测试起点后的 work/v05/calendar-final.log 为日历1项通过。不能将旧批次写成一次性全部通过。大字体 acceptance-large.log；汇总 resumed-acceptance-run.log。测试图片仅虚构样例，目录 work/v05/previews/v05-previews，部分截图处于原生弹窗淡入帧，不作发布截图。
- 独立升级实例：真实已发布v0.4.0→正式0.5.0原签名覆盖升级通过，全量记录/便签字段、旧通知渠道保持；Release启动、便签通知直达/编辑/重建通过。证据 work/v05/upgrade-final.log。V05UpgradeTest 的 seed/verify 清理自身样例，不删除原数据。
- 签名与包元数据已核对：work/v05/release-verification.json。APK12801871字节，SHA256 ac8ee5f71959a2012d53d18314f8f92818d13d47a6e09ee5f1a9cd7cc872d1ad。
- 鸿蒙6及跨品牌真机未验证。Android8–14真实授权UI和大图库未专项实测；低版本分支/路由单测不等于真机通过。提醒异常继续暂缓。

## 当前Git、交付与剩余工作

- 本轮开始基线main/3b532c6，外观代码329c64e；本轮业务074370d已推送。CI34936898741两次在Android准备步骤失败（旧tools包不存在），正提交packages: platform-tools的局部CI修复。最终状态以Git为准，不强推。
- 已生成 outputs/daybook-v0.5.0.apk、使用说明-v0.5.0.md、验收报告-v0.5.0.md。APK是已完成本地升级验收的正式包；远程发布尚待完成。
- 剩余：检查暂存并提交推送main；核对新代码CI；从发布提交归档源码并生成SHA256SUMS；按用户授权发布v0.5.0安装包、核对远程附件；更新完成记录，关闭专用模拟器并保留数据。
- README已指向计划发布的v0.5.0，发布完成前该链接可能暂不可用。
- 不自动开始backlog。新需求或用户反馈再讨论下一阶段。

## 重要设计与边界

- 单Android模块，Kotlin/Compose/ViewModel/Repository/Room，Android8.0+，无账号/网络/统计。保持本地日历语义，EVENT/TASK/NOTE固定；仅任务完成/逾期，无日期任务位于任务页。
- 便签独立memos表，不混入日历/任务/回顾；正文优先，无强制标题或日期，第一行摘要，自动保存，最多20000字。
- 回顾先编辑条件，再点击筛选；全条件默认需确认；重置清空输入/结果/错误/确认。无日期任务只进入全时间段。
- Room保留1→2→3→4增量迁移；JSON4兼容1–4。恢复完整校验/确认后先建两张表完整快照，再事务替换；失败保留原数据。旧备份会清空便签，确认提示不删除。不合并。
- 背景本机副本，预览/应用/取消/失败保护，外观不进JSON。Android8–12L读存储(max32)，13读图片，14+部分图片授权；不申请视频或所有文件权限。图片访问不保证私有/隐藏/云端原图。
- 自定义颜色可无背景，换图保留手动色，可主动跟随背景取色；实际颜色按可读性调整。放假蓝/补班红/个人事项绿/错误色保持语义。
- 提醒独立于事项日期，保留daybook.reminders渠道，不绕过系统开关。未发出的过去24小时提醒可补发，超60秒标注；系统强停需重开。RedmiK70Pro既有用户实测不能推广全部机型。
- 2025/2026假期调休内置，未覆盖年份不推测。生日循环、正文照片、完整教程、生理期、小组件、同步、AI均未授权开工。
- 不复制原始需求里的私人图片或记录进公开仓库。签名、本地配置、数据库、备份、work、outputs与构建产物保持忽略。

## 环境与已解决问题

- 构建封装 work/v04/build.sh；JDK/Gradle/SDK/Android用户目录均在work中，不重新安装环境。GitHub CLI封装 work/github_cli.py，复用认证，不输出凭据。
- 原QA实例 work/avd / emulator-5554，升级实例 work/v04/upgrade-avd / emulator-5556，名称均Daybook_API_35。升级实例现在已装正式0.5.0，不能再直接运行断言0.4.0的verify_upgrade.py或旧断言未安装的脚本。两个实例已正常关闭并保留数据；恢复前核对实际进程。
- 不卸载/清库；不操作用户手机/其他模拟器。权限不假设跨会话继承。
- 日历自动化最初从最右侧起滑，被FAB遮挡；最终改为从可见卡片中部起滑后通过。先前关于窗口时序/滚动竞争的解释不视为最终根因。
- 升级测试最初Kotlin推断非void返回，初始化失败未写样例，显式Unit后通过；一次Release增量打包失败，同一构建重试成功；测试曾调用SDK未暴露接口，已移除。
- M1原work/v05/final-build.log被本轮构建误覆盖后改名m2-release-build.log，不可将其当M1证据；M1其他日志/截图/CI仍在。
- 上轮构建自动审批因额度耗尽拒绝，未执行；用户继续后确认额度已恢复，再执行修正，无消耗重置信用。

## 额度与恢复约定

以最新个性化说明20%/15%/10%为准：高于20%正常；15%～20%保守，不开新大型阶段；10%～15%安全收尾；低于10%紧急收尾，仅保存关键文件、先更新本交接再记录Git，立即暂停，不额外QA或环境整理。

长任务适度查询，不频繁打断。恢复先读本文件、AGENTS.md与当前个性化说明，低成本核对实际Git/输出/日志；复用已通过成果，实际事实优先于旧文字。收尾第一动作是更新交接，不强行提交未验证或不稳定状态。
