# Daybook 开发约定

- 使用中文解释需求与验收结果。每次改动保持小而可理解。
- 单个 Android 模块；Compose UI / ViewModel / Repository / Room 分层，不提前引入网络或多模块框架。
- 日期保留本地日历语义。只有任务可以完成和逾期。
- 用户数据不能通过破坏性数据库迁移清除。保留 Room schema 和迁移测试。
- 备份先完整校验，再预览确认，创建恢复快照后事务替换；失败不能影响原数据。
- 新行为先确定验收预期；日期、数据库、恢复路径必须有针对性测试。
- 每完成里程碑，更新 docs/DEVELOPMENT.md，检查暂存文件，提交并推送 main；不强推。
- 仅提交代码和通用文档。不要提交真实个人记录、备份、签名材料、本地路径配置或构建产物。
- 项目目标仓库固定为 [`github.com/lukino0737/daybook`](https://github.com/lukino0737/daybook)。用户已永久授权 Codex 在该仓库执行本项目正常开发交付所需的 GitHub 上传与发布：源码归档、APK、SHA256 校验清单及对应 Release/标签。该授权覆盖后续版本，不必因每次版本重复确认；仍不得上传真实个人记录、备份、密钥、签名材料、本地配置或其他未纳入交付的敏感文件，并应保留旧版本标签与附件。
- 模拟器、真机、自动测试结果分开记录；未验证不能写成通过。
- 额度只在既定自然节点检查；以五小时窗口为准，剩余接近18%或更低时安全收尾。周额度百分比不作为暂停、收尾或缩减任务的依据；不要因周额度剩余18%停止，也不要将助手的误判称为用户要求。
