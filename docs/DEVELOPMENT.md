# 开发日志

## 初始化 · 2026-09-10

- 目的：先固定产品行为和开发约定，避免代码生成偏离范围。
- 已完成：README、第一阶段计划、AGENTS.md 与忽略规则。
- 检查：工作目录原本没有代码或 Git 仓库；已有 Git 作者配置。Android SDK 尚待安装。
- 限制：此提交尚无 APK；GitHub 浏览器连接超时，正在配置命令行托管方式。
- 学习入口：docs/PLAN.md。先掌握安排、任务和记录之间的行为差异。

## 里程碑① · 工程骨架与安装验证

- 已完成：公开仓库、Gradle Wrapper（含 SHA-256 校验）、固定依赖、原生应用骨架、图标与基础主题、CI 构建检查。
- 验证：assembleDebug 成功；Android 15 / API 35 ARM64 专用模拟器安装并通过 1 项启动测试。
- 工具链：JDK 21、Gradle 8.13、AGP 8.13.2、SDK 36、Build Tools 35.0.0。模拟器使用 macOS 虚拟化。
- 限制：尚未在用户真机安装；本里程碑界面仅用于验证启动，不包含日历功能。
- 学习入口：MainActivity → setContent；settings/build.gradle.kts → Gradle 如何组织和编译应用。

## 里程碑② · 记录闭环

- 已完成：安排/任务/记录统一表单、Room 数据库与 schema、当天列表、修改、完成/恢复、删除与短时撤销、SavedStateHandle 草稿。
- 验证：APK 构建成功；7 项日期/字段规则单元测试通过；4 项模拟器测试通过（启动、数据库重开与删除恢复、写入失败回滚、界面草稿重建与编辑保留 ID）。
- 失败验证：快照回调抛错、SQLite 触发器强制插入失败，原数据均未丢失。
- 限制：月历视图与备份入口在后续里程碑；用户真机仍待验证。
- 学习入口：EntryEditor → DaybookViewModel.save → EntryRepository.save → Room。注意保存成功后才关闭表单。

## 里程碑③ · 月历与截止提示

- 已完成：周一起始的月历、月份切换和回到今天、内容溢出数量、近期截止与全部待完成列表、未安排入口。月历优先展示未完成任务。
- 验证：9 项单元测试、5 项 API 35 模拟器测试通过；Android Lint 通过。截图检查月历、样例内容与浮动录入入口。
- 日期覆盖：2024–2030 每个月的周对齐、闰日、跨年、截止日全天与精确时间、完成任务退出提示。
- 限制：备份入口尚未接入；真实设备与大字体专项检查待后续完成。
- 学习入口：monthCells 决定日历布局；EntryRules 决定截止状态；MonthCalendar 只负责显示。

## 里程碑④ · 备份恢复与可靠性

- 已完成：系统文件选择器导出/导入、版本化 JSON、确认替换、AtomicFile 恢复快照、事务替换及快照回退。限制文件 20 MB / 10000 条，拒绝重复 ID、非法日期和缺失字段。
- 修复：备份恢复后，旧的删除撤销不能把上一份数据库的记录重新插入；后台工作显示进度。
- 验证：15 项单元测试、8 项 API 35 模拟器测试、Lint 通过；空备份必须确认，取消保持原数据，快照能够恢复。
- 视觉检查：普通字体月历；系统 150% 字体并打开软键盘时，标题、备注与保存操作可用。检查并修正浅色编辑页的状态栏图标对比度。
- 限制：尚未验证用户品牌手机的文件选择器和输入法；不包含自动备份或系统通知。
- 学习入口：BackupCodec 校验数据，BackupService 管理文件，EntryRepository.replaceAll 管理事务。三者职责不要混在 UI 里。

## 里程碑⑤ · 首次试用版交付

- 已完成：本地固定 release 签名、12 MB 左右 APK、使用与构建说明、测试报告、学习路线、样例截图、版本说明。
- 验证：release 构建和 Lint 通过；15 项单元测试通过。同签名测试基包写入样例后，覆盖安装 release，UUID、标题、备注、日期时间、完成状态与时间戳全部保留；release 启动通过。
- 分发：版本 v0.1.0 作为首次试用版；源码与文档在 main，APK 和 SHA-256 校验文件放入 GitHub Release 附件。
- 限制：工程交付与模拟器验证完成，不等于真机和两周使用验收完成。接下来以用户实际使用反馈为依据修复问题。
- 学习入口：先看 docs/LEARNING.md；从一个真实小问题开始下一次贡献。

## 第二阶段 A：独立提醒与兼容迁移（2026-09-10）

- 改动：安排/任务增加独立提醒时间、通知与准时提醒权限入口；用单个系统闹钟调度下一次提醒，触发前重新核对数据库。完成、删除、编辑、恢复都会更新调度。通知可打开记录。
- 可靠性：已发送状态持久化；24 小时内补发并标识，更旧不补发；开机、时区/时间调整、应用更新和返回前台重新核对。Room v1→v2 增量加列；备份 v2 完整校验且兼容 v1。
- 已验证：21 项 JVM 单测通过；首轮模拟器 11 项通过、2 项条件测试跳过；通知拒绝权限独立用例通过。Debug 构建与 Lint 通过（0 错误，版本提示等警告保留）。
- 修复记录：通知进入后的屏幕重建不再重新覆盖编辑草稿；新通知作为一次性导航处理，不修改 Activity 的原始启动 Intent。
- 学习入口：ReminderRules（本地时间→触发时刻），ReminderCoordinator（数据库队列→系统闹钟），EntryRepository.reconcileReminders（与编辑/恢复共用写锁），MigrationTest（用真实历史 schema 验证字段保留）。
- 真机：新增提醒行为尚未验证；系统强行停止和品牌省电策略仍需试用。生日重复不在此版本。
- 补测结果：通知实际点击打开对应记录、关闭提醒草稿在屏幕重建后保留、保存后取消通知，独立 UI 用例通过。与拒绝权限用例合计，本里程碑完成 13 项模拟器验证（两个用例单独运行）。

## 第二阶段 B：标签与回顾（2026-09-10）

- 改动：记录可添加多个标签；回顾默认按日期倒序显示生活记录，可切换全部类型，以标签和标题/正文搜索取交集；点击卡片标签可跳转筛选。编辑后保留回顾条件，在回顾中新建默认是生活记录。
- 数据：标签保存在同一条 Room 记录中（受限的 JSON 列），不引入额外模块。v2→v3 增量迁移，历史记录获得空标签；JSON v3 导出，兼容 v1/v2 导入，并严格检查新字段。
- 用户确认：手机固定竖屏；继续保证大字体、软键盘及系统重建后的草稿可用。
- 验收范围：标签标准化与限制、搜索交集及稳定排序、v1/v2 真实 schema 迁移、回顾筛选编辑、提醒与备份回归；结果在下方完成后记录。
- 学习入口：ReviewRules（先写可测试的筛选规则），EntryConverters（持久化格式），MigrationTest（历史版本兼容），ReviewUiTest（从用户操作验证需求）。
- 验证结果：27 项 JVM 单测通过；完整回归 16 项模拟器用例中 14 项通过，权限拒绝/覆盖升级两项按设计单独运行；Debug 构建与 Lint 通过。回顾截图已目视检查，标签、筛选结果和入口可见。额外的进程退出/重启提醒专项将于发布验收执行。

## 第二阶段交付与试用反馈修正（2026-09-10）

- 用户截图指出“未安排”容易误解：入口改为“无截止”，仅收没有截止日期的任务；空状态明确说明安排在日历查看。计数与列表一致（包含已完成项），“待完成”计数只统计所有未完成任务。
- 在无截止入口新建默认无日期任务，在待完成入口新建默认任务；日期与类型选择不再与所在列表矛盾。新增 UI 用例验证过滤、计数变化和默认录入。
- 交付验证：27 项单测、15 项功能模拟器用例、拒绝权限、进程退出/重启提醒、大字体与软键盘，以及真实已发布 v0.1.0 到 v0.2.0 的同签名覆盖安装通过。细节见 docs/TESTING.md。
- 版本：versionCode 2 / versionName 0.2.0；Room schema 3；JSON 格式 3（可读 1–3）。APK 仅作为 Release 附件，不入 Git。
- 下一步：用户手机开启通知与准时提醒，试用后台送达、标签检索和新入口；生日重复、照片、背景不在此版本。

- 发布确认：v0.2.0 标签指向 9bb2341，APK 与校验文件已上传且远程 digest 匹配；GitHub CI 34478904514 成功。源码归档和使用说明已保存到本地 outputs/，模拟器已停止。

## v0.3.0 A：简洁界面与显式筛选（2026-09-11）

- 按用户确认方案改为固定三入口、紧凑日期格、事项小标记、左右滑动月份、按条件显示今天和日期差、加号按钮；任务按是否有截止日期分区。
- “安排”仅显示名改成“日程”，不改 EVENT、Room schema 或 JSON 格式；只有任务可用叉号清除日期和时间。
- 回顾完整列举类型，默认全范围；编辑条件和应用条件分别保存在 SavedStateHandle 中，点击筛选才更新结果，全默认需要确认。
- 验证：30 项 JVM 单测通过；15 项常规模拟器用例中首轮 14 项通过，日历标记定位失败，改为读取未合并语义后日历独立复测通过；3 项条件专项按设计跳过。Debug 构建与 Lint 通过。目视检查紧凑月历、任务数量、底部导航及加号位置。
- 尚未交付 APK：节假日、提醒权限独立页及最终升级/截图验收仍在后续阶段。
- A 代码已提交并推送 `3a92819`。B 已核对 2025/2026 官方公告，资料保存到 HOLIDAY-SOURCES.md；额度接近安全下限，尚未开始 B 编码，先保存恢复记录并暂停。

## v0.3.0 B：内置节假日（2026-09-11）

- 完成：2025/2026 中国大陆官方放假与补班数据；常用公历及农历节日名称。蓝点放假、红点调休上班，个人事项仍是绿点；选中日期显示完整节日/调休含义，读屏也可区分。
- 实现：HolidayRules 存放可独立测试的日期规则；FestivalCalendar 使用 Android 自带 ICU，在中国时区解释本地日历日期，无新依赖或网络请求。普通周末与未覆盖年份不推测假期。
- 验证：32 项 JVM 单测通过，HolidayCalendarTest 两项及既有 CalendarUiTest 模拟器通过；Debug 构建与 Lint 通过。覆盖官方日期数量、跨月边界、春节/除夕、农历节日、手机时区变化、三色标记共存。紧凑日历截图已目视检查。
- 限制：官方调休表与清明名称覆盖 2025/2026，其他年份待 APK 更新；实际农历转换由系统 ICU 提供。真机检查未执行。
- 学习入口：区分节日本日与放假区间，不能用假期第一天代替农历节日。资料与来源见 HOLIDAY-SOURCES.md。

## v0.3.0 C：提醒入口与渠道配置（2026-09-11）

- 完成：权限集中为独立页面，左上角返回；编辑页只保留入口。删除各页重复提醒状态与手动重新检查按钮，返回前台仍自动核对调度。
- 保留 `daybook.reminders` 渠道及用户既有设置；新渠道明确采用系统通知声、高重要性和振动，声明 VIBRATE 权限。设置页显示渠道声音/振动现状并直达系统设置。
- 验证：32 项 JVM 单测、Debug 构建与 Lint 通过；完整模拟器回归 22 项中 19 项常规用例通过、3 项条件专项跳过。权限拒绝单独通过。150% 字体补测提醒页/编辑页通过，回顾卡片点击曾落在合并语义节点中心的标签上，改为点击标题本身后复测通过。
- 已验证设置页返回与页面重建保留草稿、新渠道配置正确、初始化不修改已有渠道。截图目视检查通过。
- 限制：系统配置验证不等于真机声音/振动/悬浮实测；静音及厂商策略由系统控制。最终同签名升级、截图更新和发布尚待执行。

## v0.3.0 交付验收（2026-09-11）

- 提醒功能提交 1382f83、节假日提交 036408a 均已推送，GitHub CI 通过。
- versionCode 3 / versionName 0.3.0；同签名真实 v0.2.0 覆盖升级完成，扩展样例验证每个字段、日程类型及既有通知渠道保持不变。
- Release 全新安装验证默认通知渠道重要性 4、系统声音、振动开启；两项 Release UI 用例通过。确认后台进程退出后，不重开 App，系统实际发出通知并显示顶部悬浮，已核对截图和 SystemUI 状态。
- README 第一张月历及回顾截图已替换，增加独立提醒页截图；使用、构建、验收文档与后续清单同步。截图仅为虚构样例。
- Debug/Release 构建、32 项单测、19 项常规模拟器用例、权限拒绝、大字体、覆盖升级通过。真机声音/振动/静音表现仍待用户试用，不能用模拟器配置检查代替。
- 学习入口：ReminderCoordinator.defaultChannel 说明渠道创建时的默认值；MainActivity.onResume 保留返回前台自动核对；UpgradeContinuityTest 的扩展样例验证“改界面而不丢数据”。
- APK 已就绪，SHA-256 为 1fdd6fedb3fb99ccb720d3f03eaf3fda94bad763eef8748edab0db050e875fa1。版本附件发布后记录远程结果。

- 发布确认：v0.3.0 标签指向 827709a，GitHub CI [34560985624](https://github.com/lukino0737/daybook/actions/runs/34560985624) 通过；[版本附件](https://github.com/lukino0737/daybook/releases/tag/v0.3.0) 已上传，APK 和校验文件远程 digest 核对一致。专用模拟器已停止，等待真机试用反馈。


## 首次提醒设置与多品牌后台引导（2026-09-11，尚未发布）

- 用户确认：首次启动请求通知权限，随后提供一次可跳过的提醒设置引导；面向广泛安卓设备，不能只兼容小米/红米。
- Android 13+ 仅在未授权时自动请求一次；允许、拒绝、关闭弹窗后继续引导。旧系统或已授权设备直接进入引导；跳过、返回或去设置后不重复。升级安装也会获得一次新增引导。
- 提醒设置保留通知/准时提醒，补充自启动与后台运行、悬浮通知的推荐说明；提供多品牌说明及可用时的厂商入口，失败回退到通用应用详情/系统设置。省电策略不统一要求无限制，不猜测厂商权限状态。
- 首启状态使用独立 SharedPreferences，不涉及 Room、JSON、提醒调度或旧通知渠道设置。版本号和 v0.3.0 发布附件保持原样，本阶段尚未生成新正式发布。
- 现有 Activity UI 用例通过 ReturningUserRule 明确采用已完成引导的用户状态；新增用例单独验证冷启动引导，不通过生产代码测试开关跳过流程。
- 学习入口：StartupReminderSetup（权限结果与一次性引导）、BackgroundSettingsProfile（品牌差异与通用回退）、StartupReminderSetupTest（跳过、重建、重新启动和设置返回）。
- 验证结果详见 docs/TESTING.md 的未发布改动部分；仅运行相关专项，复用 v0.3.0 既有验收。

- 完成提交 c13a6de 并推送 main，远程提交一致；GitHub CI 34588359728 通过。提醒设置预览及验证证据已保存，专用模拟器已关闭。本轮未发布新 APK。

## v0.4 A 标题与筛选重置（2026-09-13）

- 按已批准方案压缩单行年月标题和留白，统一“具体时间未定”文案。
- 筛选旁新增重置，清空输入、已应用结果、错误与确认状态，恢复待筛选；不更改日历记录。
- Debug/测试 APK 构建及 ReviewUiTest 专项通过，验证重置后条件和结果同步清空、原 3 条测试记录保留。日志 work/v04/a-*.log。

## v0.4 B 日历分页（2026-09-13）

- 使用 Compose HorizontalPager 实现跟手换月和短滑回弹，保留读屏切月、日期点击；不同月份按内容高度布局。
- 分页短滑/2月至3月/日期点击、日历实际记录与回到今天、节假日两项均通过。首次实际列表测试发现 scrollToPage 在子布局测量时同步重入，改用 requestScrollToPage 后两项相关复测通过。日志 work/v04/b-*.log。
- A/B 合并为一次稳定阶段提交；先前普通沙盒不允许写 Git 索引，源码编辑未受影响，阶段提交使用工具批准的 Git 权限。

## v0.4 C 独立便签与数据保护（2026-09-13）

- 底部增加独立便签入口，正文优先、首行摘要、默认无日期、可选日期和独立提醒；自动保存，返回确保保存，空的新便签不留库，旧便签清空正文时要求明确删除。
- Room 4 新增 memos 表，原 EntryKind 三类不变。独立通知身份与路径；统一调度投影，保持既有补发时限，编辑便签不覆盖并发写入的已发送状态。
- JSON 4 保存记录与便签，读 1–4；旧备份无便签时确认页提示清空。两张表共用快照、写锁和事务，导出读取一致快照；外观不进 JSON。
- 39 项 JVM 单测、Debug/测试 APK 构建、Lint 通过。7 项专项通过：便签 UI、真实通知跳转及重建、快照恢复/故障回滚、发送与编辑状态、两条历史迁移路径、既有备份服务。日志 work/v04/c-*.log。
- 新测试编译时曾缺少通知文案的空值判断，修正后重新构建并执行；旧测试 APK 上的两项类缺失失败不计为产品失败或通过证据。
- 尚未完成 D 背景主题、最终大字体/外观检查和正式同签名 v0.4.0 交付。


## v0.4 D 自选背景与本地配色（2026-09-13）

- 外观设置使用系统选图合同，旧系统自动回退文件选择；选图后预览再应用，可恢复默认。照片生成应用私有副本，不要求整个相册权限，不增加联网或云端服务。
- 限制输入体积与解码尺寸，处理照片方向、透明图片底色；本地直方图选取代表色并生成可读主题。四主页共享背景，编辑/设置使用清晰底色，日历语义标记与错误色不变。外观不进 JSON 记录备份。
- 使用原子配置文件，预览不改变已应用外观；图片失败、配置写入失败保留原设置，重启可恢复，取消/替换清理无用副本。未新增库或更改提醒和数据库方案。
- Debug/测试包构建、39 项 JVM 单测及 Lint（0 错误）通过。4 项外观仪器测试通过，包含独立副本、取消/重启/恢复默认/失败保护、旋转照片和旧解码路径、极端图片配色对比度。
- 四主页、便签输入/提醒/返回、外观预览应用的正常字体 1 项和 150% 字体 2 项专项通过；截图只含虚构记录，已目视检查。日志 work/v04/d-*.log；预览 work/v04/previews。正式 Release 同签名升级和系统选图端到端待交付阶段核对。
- 参考：Android 官方 [Photo picker](https://developer.android.com/training/data-storage/shared/photo-picker)、[ImageDecoder](https://developer.android.com/reference/android/graphics/ImageDecoder) 与 [ExifInterface](https://developer.android.com/reference/android/media/ExifInterface)。


## v0.4 正式交付验收（2026-09-13）

- versionCode 4 / versionName 0.4.0，Release 构建通过；签名证书与已发布 v0.3.0 相同。
- 使用独立 AVD 目录完成真实 v0.3.0 → v0.4.0 覆盖升级，记录全部字段和旧通知渠道保留；正式包启动及便签通知直达/编辑重建通过。原 QA 模拟器的数据和安装保留。
- 补验 150% 字体加软键盘下便签输入、滚动提醒开关和返回保存，通过。README/使用说明/备份边界及虚构样例截图同步更新。
- D 阶段 fd36ad0 的 CI 34761242134 成功。未重复完整 QA，旧提醒异常长期复现与跨品牌真机试用仍待用户反馈。
- 正式包系统选图端到端通过：选取虚构图片、预览、应用、重启保留、恢复默认；截图 work/v04/release-picker-*.png。
- 交付代码 8e03e3b 已推送，CI 34762134277 成功。本地 APK/源码/校验/说明/预览已交付。创建 v0.4.0 远程标签和公开 Release 的自动审批被拒绝（尚无此次公开发布的明确授权），发布命令未执行；待用户确认，不能换途径绕过。两个专用模拟器均已关闭并保留数据。
- 用户随后明确授权公开发布；v0.4.0 标签（2020711）与 Release 创建成功、附件上传完成。最后远程 digest 核对被自动审批以额度耗尽拒绝，未执行；按额度规则保存本地交接后安全暂停。恢复只补远程核对和文档提交，发布授权无需重问。


## v0.4 发布收尾完成（2026-09-14）

- 先读取 CODEX_HANDOFF.md 与本机 Codex 个性化说明，仅更新 handoff 额度段落为 15%/10%/5% 分档，明确进入收尾后优先写交接。遵照用户限制保留其他交接内容，其中上一轮“远程校验待补”的历史状态由本节和 TESTING 本次完成记录补充。
- 复用已发布成果，核对远程 APK、源码归档、SHA256SUMS 三个附件的 SHA-256 和大小，均与本地一致；v0.4.0 远程标签解引用为 20207119374e724e73b1b1e6b302da4bb99b9098，符合预期。证据 work/v04/published-release.json。
- 仅整理并提交上轮保存的发布状态和本轮收尾文档；业务代码未变，不重新构建、上传、移动标签或完整 QA。此前代码 CI 34762134277 成功结果直接复用。发布与收尾已完成，等待用户下一步需求，不启动 backlog。


## 新对话交接整理（2026-09-14）

- 低成本核对 main/1f1f764 的干净状态、v0.4.0 标签、版本配置、已有验收日志与交付文件；本地 APK/源码/校验文件与保存的远程证据及任务输出副本一致。
- 用户明确要求全面整理交接后，更新 CODEX_HANDOFF.md：移除过时的“远程校验待补”恢复指令，补齐目标、阶段、设计决策、弃用方向、风险、Git/输出、关键环境、优先级及新对话检查步骤；保留最新 15%/10%/5% 额度规则。
- 没有修改业务代码、重建、完整 QA、启动模拟器或改动发布附件。v0.4.0 已发布且全部收尾完成，交接后停止，等待用户下一步需求。


## v0.5.0 里程碑一：相册与调色盘（2026-09-14）

- 按用户确认的 A 方案增加分版本相册权限、应用内相册分组/缩略图选图、部分授权补选、拒绝后系统选图与文件入口、返回前台刷新权限。
- 增加完整色相圆盘、饱和度/明度调节和常用色，支持无背景独立配色与切回背景取色；旧外观配置兼容，原子保存与取消保护保留。删除圈出的外观说明文字。
- 开发版本 0.5.0-dev / code 5；Room/JSON 保持 4，无新增库。Debug/AndroidTest 构建、41 项 JVM 单测、Lint（0 错误）通过；API 35 专用模拟器 7 项外观存储/UI 专项及权限/相册操作验证通过。详见 V0.5-M1.md。
- 仅提交并推送本里程碑，不创建 v0.5.0 标签或 Release，不发布最终 APK。旧提醒异常继续暂缓，鸿蒙 6 与其他厂商真机未验证。
- 里程碑代码 `329c64e` 已推送 main；[CI 34822871560](https://github.com/lukino0737/daybook/actions/runs/34822871560) 全部成功。专用模拟器已关闭并保留数据；后续仅同步完成记录，等待下一步需求。


## v0.5 里程碑后的新对话交接（2026-09-14）

- 只做交接：核对 main/cd4aeaa 干净状态、开发版本 0.5.0-dev / code 5、已有 41 项 JVM 结果、7 项外观专项、成功构建与 CI 日志，以及测试截图和 v0.4 交付文件。
- 统一 CODEX_HANDOFF.md 中旧 v0.4 的版本号、Git 状态与已过时外观方案；整合当前成果、授权边界、未验证范围、鸿蒙真机信息、关键文件与恢复顺序。v0.5 里程碑一已完成，无未完成实现或验证尾项，等待新需求。
- 本轮不重建、不重跑 QA、不启动模拟器、不改业务代码或发布附件。仅提交推送交接文档，完成后停止开发。


## v0.5.0 操作体验与交付准备（2026-09-15）

- 用户确认方案A，继续使用0.5.0并要求完成后发布安装包；追加移除空正文弹窗「继续编辑」，已落实。
- 增加已完成任务分区、勾选划线及400毫秒归类过渡、恢复未完成；任务/便签/日历当天事项左滑确认删除；过去日期默认记录；便签完成退出、底部删除和安全空白退出。无新依赖，Room/JSON均保持4。
- 43项JVM、Lint、Debug/AndroidTest/Release通过；15项正常字号专项分批通过，另1项150%字号预览通过。日历测试因FAB遮挡手势起点曾失败，修正起点后通过，详见V0.5-M2.md。
- 真实v0.4.0→0.5.0/code6同签名覆盖升级、全部记录/便签字段与旧渠道保留、正式启动及便签通知/重建通过。提醒异常与鸿蒙真机继续暂缓。
- 本轮业务与文档待提交推送/CI及安装包发布；既有v0.4.0附件不动。


## v0.5 发布前CI环境修复（2026-09-15）

- 业务提交074370d已推送。CI34936898741两次在setup-android准备环境时失败，尚未执行构建；日志明确为sdkmanager找不到旧tools包。
- 根据[Action输入定义](https://github.com/android-actions/setup-android/blob/v3/action.yml)显式设置packages: platform-tools，覆盖默认tools platform-tools。保留后续Android36平台和35.0.0构建工具安装，不改业务代码/依赖版本，不重建已验收APK。
- 本地验收结果仍复用；等待修复提交的远程检查通过后发布。


## v0.5.0 发布与收尾完成（2026-09-15）

- CI修复cd35b24已推送，[CI34937365482](https://github.com/lukino0737/daybook/actions/runs/34937365482)全部成功；此前旧tools包导致的环境准备失败已解除。
- 按用户授权创建v0.5.0正式Release，标签指向cd35b246b7f848fafc9f8d004f7017cb02d3de78，上传原签名APK、源码归档与SHA256SUMS。
- 远程三个附件的大小/SHA256与本地逐项一致，标签目标正确；证据work/v05/published-release.json。两个专用模拟器正常关闭，数据保留。源码归档包含发布前交接快照，最新完成记录见main。
- 本轮实现、验收、提交推送、发布和收尾均完成，等待用户新需求，不启动backlog。


## v0.5.0 最后文档收尾恢复（2026-09-15）

- 上轮APK、源码与校验文件已发布且远程核对完成，最后四份文档暂存被额度限制的自动审批拒绝，未执行。
- 用户继续后仅核对Git、保存的发布证据和CI结果，复用全部已验证成果；同步当前个性化额度规则，提交推送已保存的收尾文档。未重建、重跑QA、改动标签或替换发布附件。

## v0.5.0 新对话交接整理（2026-09-15）

- 用户要求结束当前对话，仅整理交接。核对main与本地origin/main均为940bd95，开始时工作区干净；读取现有构建/验收日志、43项全部通过的JVM结果及已保存的正式发布附件证据。
- 更新CODEX_HANDOFF.md，补齐未采用方案、已知风险、关键路径、优先级和新对话恢复顺序；更正上一轮文档未提交的过时描述（已于940bd95完成）。
- 本次只修改交接及开发记录，没有新功能、构建、测试重跑或再次发布。v0.5.0/code6交付已完成，提醒异常与鸿蒙真机验证继续暂缓，接手后等待用户需求。

## v0.6 第一批里程碑一：提醒规则与数据（2026-09-21）

- 按已批准计划开始 0.6.0-dev/code7；增加独立提醒日期规则、Room5增量迁移、JSON5兼容读取和三类数据快照/事务恢复。旧备份清空独立提醒的预览提示已加入。
- 49项JVM测试、Lint、Debug/AndroidTest构建通过；API35专用模拟器8项迁移、备份、写入状态和事务失败专项通过。初轮设备测试发现草稿旧发送状态影响修改时间，已修复并复验。
- 证据 work/v06/m1-final-build.log、m1-final-device.log；无真机验证、正式包覆盖升级或发布。下一步为调度/引导，再完成页面。

## v0.6 第一批里程碑二：调度与后台引导（2026-09-21）

- 里程碑一df9f149的CI35557870291成功。接入独立提醒调度，区分下一次时刻与已发送发生项；通知按来源/计划版本/发生时刻识别。暂停/规则编辑取消旧通知，改标题不重发。
- 更新自启动/无限制说明、应用电池设置入口和旧安装一次性补充引导。49项JVM、Lint、Debug/AndroidTest构建通过。
- API35：7项发送/兼容/引导测试通过，外部撤权/恢复各1项通过，重启seed/verify各1项通过；驱动在未打开App前确认收到系统通知。直接在测试进程撤销精确闹钟权限会导致系统终止进程，已改为外部驱动验证，未绕过该行为。
- 证据work/v06/m2-final-build.log、m2-acceptance.log及分项日志。通知直达编辑页随里程碑三页面一起验收；无真机或发布。

- 第二里程碑0f330ce的CI35558363397成功。本轮按额度规则安全收尾，第三里程碑仅有本地未接线/未编译草稿，恢复步骤已写入CODEX_HANDOFF.md；完整第一批尚未完成。


## v0.6 第一批里程碑三：界面与日历（2026-09-21）

- 从707c101及三个已保存草稿继续；前两阶段CI和设备验证直接复用。新增独立提醒编辑、统一来源列表、新建类型切换保留标题/备注、暂停与确认删除、日历按所查看月份计算发生项；普通事项后单列提醒分区，不加入任务数量/回顾。
- 接入真实通知直达与目标已删除提示；正在编辑时保留草稿，保存或取消后再打开通知。验证页面重建仍保留未保存输入。
- 修正草稿间隔输入与保存值不一致、已发送单次提醒的预览、浅色界面的状态栏对比度；失败提示保留输入且不暴露底层SQLite错误。
- 53项JVM、Lint 0错误、Debug/AndroidTest构建通过。API35正常字号19种场景分批通过：初次组合18通过1项旧日历删除确认框断言失败，相同用例独立复验通过；最后日历局部调整后正常3项复验通过。150%字号3项通过，日历局部调整后另1项大字号复验通过。失败批次原日志保留，不声称首次一次性全过。
- 正常/150%字号的编辑、列表、保存失败和删除确认截图已目视检查。使用虚构样例和隔离数据库；未操作真机，未验证正式签名包覆盖升级，未发布/交付安装包。
- 证据work/v06/m3-final-layout-build.log、m3-normal.log、m3-calendar-isolated.log、m3-calendar-final.log、m3-large.log、m3-calendar-large-final.log及previews-final/。本里程碑38df5cb已推送main，[CI35577995029](https://github.com/lukino0737/daybook/actions/runs/35577995029)成功（证据work/v06/m3-ci-final.json）。
- 本机默认Git触发Xcode许可提示，使用既有Command Line Tools（命令级DEVELOPER_DIR）完成Git操作，未变更系统许可或安装环境。

- 第一批三个里程碑已完成，最后仅同步完成记录。专用API35模拟器已恢复字号1.0并关闭，保留数据。不交付安装包、不创建正式发布、不改动v0.5.0附件；正式签名包覆盖升级及鸿蒙6真机尚未验证。停止，等待用户下一步需求。

## v0.6 图片批次里程碑一：正文结构与文件管理（2026-09-22）

- 用户确认正文内插图、图片→隐私顺序，取消便签拖动排序。保留单模块，无新依赖；范围和验收预期见V0.6-IMAGES.md。
- Room5→6为事项和便签增加有序正文块，旧文本无损保留；文字摘要校验、仅图片便签、9张限制和插入/移除规则落地。
- 图片保存为有校验值的独立本地副本，限制输入/像素/输出大小，校正方向，保留透明效果；引用/草稿保护和无引用文件清理已接入底层。尚未开放编辑入口，旧JSON导出拒绝携带图文以避免漏图。
- 56项JVM、Lint、Debug/AndroidTest构建通过；API35专用模拟器8项图片存储、5→6/历史迁移及事务失败专项通过。证据work/v06-images/m1-build.log、m1-device.log。
- 无真机/正式签名升级验收或发布；下一阶段为完整图片备份恢复。

## v0.6 图片批次里程碑二：完整备份恢复（2026-09-22）

- 数据里程碑1565c1c已推送，CI35676376546成功（work/v06-images/m1-ci-final.json）。
- 默认导出ZIP，版本6 JSON清单包含图文顺序与图片元数据；按哈希保存图片，重复引用只存一份。继续读JSON1–5；预览列出图片数量/体积、旧备份移除图片提示。
- 导出先生成完整本地包，缺图直接失败。导入在隔离目录完整校验大小、路径、引用和SHA256；确认后创建含图片快照，准备不可变图片，再事务替换。快照不依赖当前图片文件，旧图片清理后仍可恢复。
- 57项JVM、构建/Lint通过；API35八项图片备份/旧备份/事务专项通过。初轮7过1失败：系统先拒绝非法ZIP路径，异常类型与测试预期不符；统一为备份错误后八项全过。日志m2-device.log保留，最终证据work/v06-images/m2-final-build.log、m2-final-device.log。
- 编辑查看入口尚未接入；继续里程碑三。无正式包升级、真机或发布。

## v0.6 图片批次里程碑三：图文编辑与查看（2026-09-22）

- 备份里程碑4fe10e8已推送，CI35676807047成功（work/v06-images/m2-ci-final.json）。
- 三类事项和便签接入光标处插图、有序文字/图片块、确认移除、大图缩放与切换；仅图片便签显示图片数量摘要。事项保存/取消与便签自动保存保持原有语义；独立提醒不支持图片，含图的新事项禁用切换为提醒。
- 草稿、导入过程及删除撤销保护图片引用；保存成功后再清理无引用文件。取消编辑、保存/删除失败、共享图片及Activity重建均有专项。图片源文件不修改；备份包含处理后的本地副本。
- 调整预览图片尺寸、保留便签自动聚焦，修正编辑页状态栏/导航栏对比度；恢复前新增完整解码校验，拒绝校验值正确但内容不可解码的图片。没有修改日期/提醒规则或引入新依赖。
- 57项JVM、Debug/AndroidTest/Lint通过；图文/存储13种正常场景、真实系统选图1项、受影响旧流程13种场景分批通过，150%字号2项通过。失败及复验事实详见TESTING，尤其旧日历组合测试的偶发确认框失败未认定根因。
- 正常与大字号截图已目视检查；真实DocumentsUI选图、删除相册原图后保留副本、真实Activity重建通过。证据work/v06-images/，不重复第一批完整QA。
- 本地实施和验收完成，代码1a776fd已推送main，[CI35695278214](https://github.com/lukino0737/daybook/actions/runs/35695278214)成功（work/v06-images/m3-ci-final.json）。专用模拟器已恢复字号1.0并正常关闭，数据保留。不交付正式包、不发布。图片批次已完成并停止；下一批隐私便签须另行确认，排序已取消。

## v0.6.0 正式发布准备（2026-09-22）

- 用户取消隐私便签，授权修改任务默认截止日期并发布v0.6.0安装包；排序已取消，不扩展其他功能。
- 任务页新建、日程/记录切换为新任务、独立提醒切换为新任务均默认「不设截止日期」，同时清除自动带入的时间。已有任务编辑保留原日期；任务草稿经提醒页返回仍保留自行设置的截止时间。
- 版本由0.6.0-dev转为0.6.0/code7，沿用原签名、Room6与ZIP JSON6，无新依赖或数据结构变更。
- 构建Debug/AndroidTest/Release、57项JVM与Lint通过（0错误、30项既有警告）；三项API35任务/提醒专项一次通过。图片与独立提醒批次已有验收复用，不重跑完整QA。
- 真实已发布v0.5 APK基线哈希一致；原签名v0.6覆盖升级通过，Room4→6后全部旧事项与便签字段、现有外观文件哈希和通知渠道保持。seed、verify、正式启动各一项通过，测试只清理自身虚构样例，无卸载/清库。
- 证据work/v06-release/build.log、targeted.log、upgrade-seed.log、upgrade-verify.log、release-smoke.log、release-verification.json。下一步提交推送、确认CI后发布；v0.5标签/附件保持不变。鸿蒙6及其他品牌真机未验证，长期提醒异常仍暂缓。

## v0.6.0 发布与收尾完成（2026-09-22）

- 发布提交e6818e79dde42395db50df78a578363cd7e98461已推送main，[CI35722090962](https://github.com/lukino0737/daybook/actions/runs/35722090962)成功，标签v0.6.0固定指向该提交。
- 已公开[Daybook v0.6.0](https://github.com/lukino0737/daybook/releases/tag/v0.6.0)，原签名APK、源码归档和SHA256SUMS三附件大小/哈希与本地逐项一致。草稿按tag查询API曾返回404；公开发布后最终校验全部通过。v0.5标签、正文和原附件ID/大小/哈希均保持不变。
- APK SHA256：60e6ac10f3115b17a51465931047cae24dfd84b988afb86a9b115aa9781a88ba。源码来自发布提交，最终交接更新仅在main文档中追加，不修改业务或重建APK。
- 两个专用模拟器字号1.0并正常关闭，数据保留。最终证据work/v06-release/ci-final.json、published-release.json、emulator-stop.log。本轮全部完成，停止开发，等待用户新需求；隐私便签与排序均已取消。
