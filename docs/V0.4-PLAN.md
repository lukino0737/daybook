# v0.4.0 已批准实施方案

用户已确认并授权实施（2026-09-13）。本轮不排查既有提醒延迟，不启动其他 backlog。

1. 单行年月标题，减少标题留白；“时间未定”统一改为“具体时间未定”。筛选旁增加重置按钮：清空条件和搜索词、已应用结果、错误和确认状态，回到待筛选状态。
2. 月历采用跟手横向分页，滑动不足回弹，保留日期点击、大字体与读屏操作。
3. 底部四入口：日历、任务、便签、回顾。便签独立数据，不是第四种记录类型，不混入日历/任务/回顾。正文优先、无强制标题、首行摘要、默认无日期、可选日期和独立提醒、自动保存、返回确保保存、空白不保存、失败明确提示、按最近修改排序、编辑删除、通知直达便签。
4. Room 增加独立便签表并增量迁移；JSON 格式升级，备份原记录、便签和提醒，兼容旧备份。旧备份整体替换将清空便签，确认页明确提示；快照包含便签。外观和图片是本机偏好，不纳入 JSON。
5. 菜单“外观设置”：选图→背景及配色预览→应用、恢复默认。本机保存图片副本并本地提取配色。四个主页共享背景，编辑/设置用同一主题的清晰底色；按钮、选中态与强调色跟随主题，日历语义标记及错误色保留。首版无手动调色面板。
6. 分阶段针对性验证与提交推送，最终整理预览、同签名 v0.4.0 APK 和验收记录。保留上轮首次提醒设置引导，复用其既有验证。

阶段状态：A/B 8ec8e4c、C cfaec74、D fd36ad0 已提交推送并通过 CI。全部功能、专项测试、大字体/软键盘、正式 Release 构建与真实 v0.3.0 → v0.4.0 同签名升级已完成；本地 APK、源码、校验文件、预览及说明已就绪。交付代码 8e03e3b 的 CI 34762134277 成功；用户随后明确授权，远程 v0.4.0 标签/Release 已成功创建；2026-09-14 远程三个附件的摘要/大小及标签均已核对通过，剩余收尾已完成，等待用户下一步需求。
